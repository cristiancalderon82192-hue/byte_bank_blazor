@page "/titulares"
@using ByteBank.Services
@using ByteBank.Models
@inject TitularService TitularService
@inject CuentaService CuentaService
@inject CuentahabienteService CuentahabienteService

<PageTitle>Gestión de Titulares</PageTitle>

<h1>Gestión de Titulares</h1>
<p class="text-muted">Asociar y administrar titulares de cuentas</p>

<div class="row">
    <!-- Asociar nuevo titular -->
    <div class="col-md-6 mb-4">
        <div class="card">
            <div class="card-header bg-primary text-white">
                <h5 class="mb-0">Asociar Titular a Cuenta</h5>
            </div>
            <div class="card-body">
                <div class="mb-3">
                    <label>Numero de Cuenta:</label>
                    <select class="form-select" @bind="idCuentaAsociar">
                        <option value="0">-- Seleccione una Cuenta --</option>
                        @if (allCuentas != null)
                        {
                            foreach (var cuenta in allCuentas)
                            {
                                <option value="@cuenta.IdCuenta">@cuenta.Numero</option>
                            }
                        }
                    </select>
                </div>
                <div class="mb-3">
                    <label>Documento del Cuentahabiente:</label>
                    <select class="form-select" @bind="idCuentahabienteAsociar">
                        <option value="0">-- Seleccione un Titular --</option>
                        @if (allCuentahabientes != null)
                        {
                            foreach (var titular in allCuentahabientes)
                            {
                                <option value="@titular.IdCuentahabiente">@titular.Documento - @titular.Nombre</option>
                            }
                        }
                    </select>
                </div>
                <button class="btn btn-success" @onclick="AsociarTitular">
                    <span class="bi bi-link-45deg"></span> Asociar
                </button>
            </div>
        </div>
    </div>

    <!-- Remover titular -->
    <div class="col-md-6 mb-4">
        <div class="card">
            <div class="card-header bg-danger text-white">
                <h5 class="mb-0">Remover Titular de Cuenta</h5>
            </div>
            <div class="card-body">
                <div class="mb-3">
                    <label>Numero de Cuenta:</label>
                    <select class="form-select" @bind="idCuentaRemover">
                        <option value="0">-- Seleccione una Cuenta --</option>
                        @if (allCuentas != null)
                        {
                            foreach (var cuenta in allCuentas)
                            {
                                <option value="@cuenta.IdCuenta">@cuenta.Numero</option>
                            }
                        }
                    </select>
                </div>
                <div class="mb-3">
                    <label>Documento del Cuentahabiente:</label>
                    <select class="form-select" @bind="idCuentahabienteRemover">
                        <option value="0">-- Seleccione un Titular --</option>
                        @if (allCuentahabientes != null)
                        {
                            foreach (var titular in allCuentahabientes)
                            {
                                <option value="@titular.IdCuentahabiente">@titular.Documento - @titular.Nombre</option>
                            }
                        }
                    </select>
                </div>
                <button class="btn btn-danger" @onclick="RemoverTitular">
                    <span class="bi bi-x-circle"></span> Remover
                </button>
            </div>
        </div>
    </div>

    <!-- Consultar titulares de cuenta -->
    <div class="col-md-6 mb-4">
        <div class="card">
            <div class="card-header bg-info text-white">
                <h5 class="mb-0">Titulares de una Cuenta</h5>
            </div>
            <div class="card-body">
                <div class="mb-3">
                    <label>Número de Cuenta:</label>
                    <div class="input-group">
                        <select class="form-select" @bind="idCuentaConsultar">
                            <option value="0">-- Seleccione una Cuenta --</option>
                            @if (allCuentas != null)
                            {
                                foreach (var cuenta in allCuentas)
                                {
                                    <option value="@cuenta.IdCuenta">@cuenta.Numero</option>
                                }
                            }
                        </select>
                        <button class="btn btn-info" @onclick="ConsultarTitulares">
                            <span class="bi bi-search"></span> Consultar
                        </button>
                    </div>
                </div>
                @if (listaTitulares != null && listaTitulares.Any())
                {
                    <div class="table-responsive">
                        <table class="table table-sm table-striped">
                            <thead>
                                <tr>
                                    <th>ID Cuentahabiente</th>
                                    <th>Nombre</th>
                                    <th>Documento</th>
                                </tr>
                            </thead>
                            <tbody>
                                @foreach (var titular in listaTitulares)
                                {
                                    <tr>
                                        <td>@titular.IdCuentahabiente</td>
                                        <td>@titular.Nombre</td>
                                        <td>@titular.Documento</td>
                                    </tr>
                                }
                            </tbody>
                        </table>
                    </div>
                }
                else if (listaTitulares != null)
                {
                     <div class="alert alert-warning p-2">No se encontraron titulares.</div>
                }
            </div>
        </div>
    </div>

    <!-- Consultar cuentas de titular -->
    <div class="col-md-6 mb-4">
        <div class="card">
            <div class="card-header bg-warning text-dark">
                <h5 class="mb-0">Cuentas de un Titular</h5>
            </div>
            <div class="card-body">
                <div class="mb-3">
                    <label>Documento del Titular:</label>
                    <div class="input-group">
                        <select class="form-select" @bind="idCuentahabienteConsultar">
                            <option value="0">-- Seleccione un Titular --</option>
                            @if (allCuentahabientes != null)
                            {
                                foreach (var titular in allCuentahabientes)
                                {
                                    <option value="@titular.IdCuentahabiente">@titular.Documento - @titular.Nombre</option>
                                }
                            }
                        </select>
                        <button class="btn btn-warning" @onclick="ConsultarCuentas">
                            <span class="bi bi-search"></span> Consultar
                        </button>
                    </div>
                </div>
                @if (listaCuentas != null && listaCuentas.Any())
                {
                    <div class="table-responsive">
                        <table class="table table-sm table-striped">
                            <thead>
                                <tr>
                                    <th>ID Cuenta</th>
                                    <th>Número</th>
                                    <th>Saldo</th>
                                </tr>
                            </thead>
                            <tbody>
                                @foreach (var cuenta in listaCuentas)
                                {
                                    <tr>
                                        <td>@cuenta.IdCuenta</td>
                                        <td>@cuenta.Numero</td>
                                        <td>@cuenta.Saldo.ToString("C2")</td>
                                    </tr>
                                }
                            </tbody>
                        </table>
                    </div>
                }
                else if (listaCuentas != null)
                {
                    <div class="alert alert-warning p-2">No se encontraron cuentas.</div>
                }
            </div>
        </div>
    </div>
</div>

@if (mensaje != null)
{
    <div class="alert @(esError ? "alert-danger" : "alert-success") alert-dismissible fade show" role="alert">
        @mensaje
        <button type="button" class="btn-close" @onclick="() => mensaje = null"></button>
    </div>
}

<div class="alert alert-info mt-4">
    <h6 class="alert-heading">Información Importante:</h6>
    <ul class="mb-0">
        <li>Una cuenta puede tener múltiples titulares</li>
        <li>Un cuentahabiente puede ser titular de múltiples cuentas</li>
        <li>No se puede remover el único titular de una cuenta</li>
        <li>Los IDs deben corresponder a registros existentes</li>
    </ul>
</div>

@code {
    private int idCuentaAsociar;
    private int idCuentahabienteAsociar;
    private int idCuentaRemover;
    private int idCuentahabienteRemover;
    private int idCuentaConsultar;
    private int idCuentahabienteConsultar;
    
    private List<Cuentahabiente>? listaTitulares;
    private List<Cuenta>? listaCuentas;

    private List<Cuenta>? allCuentas;
    private List<Cuentahabiente>? allCuentahabientes;
    
    private string? mensaje;
    private bool esError;

    protected override async Task OnInitializedAsync()
    {
        allCuentas = await CuentaService.GetCuentasAsync();
        allCuentahabientes = await CuentahabienteService.GetCuentahabientesAsync();
    }

    private async Task AsociarTitular()
    {
        var exito = await TitularService.AsociarTitularAsync(idCuentaAsociar, idCuentahabienteAsociar);
        if (exito)
        {
            mensaje = $"Titular asociado exitosamente a la cuenta {idCuentaAsociar}";
            esError = false;
            idCuentaAsociar = 0;
            idCuentahabienteAsociar = 0;
        }
        else
        {
            mensaje = "Error al asociar titular. Verifique que los IDs existan y que no esté duplicado.";
            esError = true;
        }
    }

    private async Task RemoverTitular()
    {
        var exito = await TitularService.RemoverTitularAsync(idCuentaRemover, idCuentahabienteRemover);
        if (exito)
        {
            mensaje = $"Titular removido exitosamente de la cuenta {idCuentaRemover}";
            esError = false;
            idCuentaRemover = 0;
            idCuentahabienteRemover = 0;
        }
        else
        {
            mensaje = "Error al remover titular. Verifique que sea el último titular de la cuenta.";
            esError = true;
        }
    }

    private async Task ConsultarTitulares()
    {
        if (idCuentaConsultar == 0)
        {
            mensaje = "Por favor seleccione una cuenta.";
            esError = true;
            return;
        }

        listaTitulares = await TitularService.GetTitularesByCuentaAsync(idCuentaConsultar);
        if (listaTitulares != null)
        {
            mensaje = null;
            if (!listaTitulares.Any())
            {
                 mensaje = $"No se encontraron titulares para la cuenta seleccionada";
                 esError = true; // Or just warning
            }
        }
        else
        {
            mensaje = $"Error al consultar titulares para la cuenta seleccionada";
            esError = true;
            listaTitulares = null;
        }
    }

    private async Task ConsultarCuentas()
    {
        if (idCuentahabienteConsultar == 0)
        {
            mensaje = "Por favor seleccione un titular.";
            esError = true;
            return;
        }

        listaCuentas = await TitularService.GetCuentasByCuentahabienteAsync(idCuentahabienteConsultar);
        if (listaCuentas != null)
        {
            mensaje = null;
            if (!listaCuentas.Any())
            {
                 mensaje = $"No se encontraron cuentas para el cuentahabiente seleccionado";
                 esError = true;
            }
        }
        else
        {
            mensaje = $"Error al consultar cuentas para el cuentahabiente seleccionado";
            esError = true;
            listaCuentas = null;
        }
    }
}