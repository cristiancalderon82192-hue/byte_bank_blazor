@page "/cuentas"
@using ByteBank.Models
@using ByteBank.Services
@inject CuentaService CuentaService

<PageTitle>Cuentas Bancarias</PageTitle>

<h1>💳 Cuentas Bancarias</h1>

@if (cuentas == null)
{
    <p><em>Cargando cuentas...</em></p>
}
else if (!cuentas.Any())
{
    <div class="alert alert-info">
        <strong>No hay cuentas registradas.</strong>
    </div>
}
else
{
    <div class="mb-3">
        <strong>Total de cuentas:</strong> @cuentas.Count
    </div>

    <table class="table table-striped table-hover">
        <thead class="table-dark">
            <tr>
                <th>ID</th>
                <th>Número de Cuenta</th>
                <th>Fecha Apertura</th>
                <th>Saldo</th>
                <th>Sobregiro</th>
                <th>Acciones</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var cuenta in cuentas)
            {
                <tr>
                    <td>@cuenta.IdCuenta</td>
                    <td><strong>@cuenta.Numero</strong></td>
                    <td>@cuenta.FechaApertura.ToShortDateString()</td>
                    <td class="text-success">
                        <strong>$@cuenta.Saldo.ToString("N2")</strong>
                    </td>
                    <td>
                        @if (cuenta.Sobregiro > 0)
                        {
                            <span class="text-warning">$@cuenta.Sobregiro?.ToString("N2")</span>
                        }
                        else
                        {
                            <span class="text-muted">Sin sobregiro</span>
                        }
                    </td>
                    <td>
                        <button class="btn btn-sm btn-primary" @onclick="() => VerDetalle(cuenta.IdCuenta)">
                            Ver Detalle
                        </button>
                        <button class="btn btn-sm btn-danger" @onclick="() => EliminarCuenta(cuenta.IdCuenta)">
                            Eliminar
                        </button>
                    </td>
                </tr>
            }
        </tbody>
    </table>
}

@if (mensaje != null)
{
    <div class="alert @(esError ? "alert-danger" : "alert-success") alert-dismissible fade show" role="alert">
        @mensaje
        <button type="button" class="btn-close" @onclick="() => mensaje = null"></button>
    </div>
}

@code {
    private List<Cuenta>? cuentas;
    private string? mensaje;
    private bool esError;

    protected override async Task OnInitializedAsync()
    {
        await CargarCuentas();
    }

    private async Task CargarCuentas()
    {
        cuentas = await CuentaService.GetCuentasAsync();
    }

    private void VerDetalle(int idCuenta)
    {
        // Navegar a página de detalle (implementar después)
        mensaje = $"Detalle de cuenta {idCuenta}";
        esError = false;
    }

    private async Task EliminarCuenta(int idCuenta)
    {
        if (confirm($"¿Está seguro de eliminar la cuenta {idCuenta}?"))
        {
            var exito = await CuentaService.DeleteCuentaAsync(idCuenta);
            if (exito)
            {
                mensaje = "Cuenta eliminada exitosamente";
                esError = false;
                await CargarCuentas();
            }
            else
            {
                mensaje = "Error al eliminar la cuenta";
                esError = true;
            }
        }
    }

    private bool confirm(string message)
    {
        // En producción, usar un modal de confirmación
        return true;
    }
}